services:
  zookeeper:
    image: zookeeper:3.8
    container_name: chaos-zk
    ports:
      - "2181:2181"
    environment:
      # 放开四字命令白名单，方便用 `echo stat | nc localhost 2181` 自检
      ZOO_4LW_COMMANDS_WHITELIST: "*"
