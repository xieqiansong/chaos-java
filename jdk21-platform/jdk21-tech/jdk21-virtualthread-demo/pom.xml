<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>lan.chaos</groupId>
        <artifactId>jdk21-tech</artifactId>
        <version>1.0-SNAPSHOT</version>
    </parent>

    <artifactId>jdk21-virtualthread-demo</artifactId>
    <packaging>jar</packaging>
    <name>virtualthread-demo</name>
    <description>虚拟线程：机制演示（吞吐对比 / 载体线程挂载卸载 / synchronized pinning / 结构化并发 / ThreadLocal 语义）+ 压测量化（IO 密集 / 饱和拒绝 / pinning 代价 / CPU 边界 / HTTP 服务）</description>

    <properties>
        <!-- 默认不跑压测矩阵（分钟级）；用 mvn test -Dbench=true 显式触发 -->
        <bench>false</bench>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <!-- StructuredTaskScope 在 JDK 21 为预览 API（JEP 453），需启用预览 -->
                    <compilerArgs>
                        <arg>--enable-preview</arg>
                    </compilerArgs>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <argLine>--enable-preview</argLine>
                    <!-- 把 -Dbench 透传给测试 JVM，使 @EnabledIfSystemProperty 生效 -->
                    <systemPropertyVariables>
                        <bench>${bench}</bench>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>${spring-boot.version}</version>
                <configuration>
                    <mainClass>lan.chaos.virtualthread.VirtualThreadApplication</mainClass>
                    <!-- 运行主线演示同样需要预览开关 -->
                    <jvmArguments>--enable-preview</jvmArguments>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
