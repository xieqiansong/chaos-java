registry {
  # file 注册中心：无需额外中间件（如 Nacos），Seata Server 与客户端都直接读本地 conf。
  # 演示分布式事务最轻量的接入方式；生产常用 nacos / eureka 做注册发现。
  type = "file"
  file {
    name = "file.conf"
  }
}

config {
  type = "file"
  file {
    name = "file.conf"
  }
}
