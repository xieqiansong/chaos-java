# 测试环境统一关闭 Nacos Config：离线跑单测无需真实 Nacos。
# import-check 也关掉，避免 spring-cloud 要求必须配置 spring.config.import
spring:
  cloud:
    nacos:
      config:
        enabled: false
        import-check:
          enabled: false
      discovery:
        enabled: false
